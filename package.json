{
  "name": "ai-input-history-extension",
  "version": "1.25.0",
  "private": true,
  "description": "Local-first input history for AI chat websites",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/validate.mjs",
    "validate:store": "node scripts/validate-store.mjs",
    "assets:store": "python scripts/generate-store-assets.py",
    "package:store": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/package-store.ps1"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
